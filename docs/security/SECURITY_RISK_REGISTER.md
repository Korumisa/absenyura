# Security Risk Register

This is a living document. Items marked as **Needs Verification** are hypotheses derived from static review and must be confirmed via tests/scans before being treated as vulnerabilities.

|     ID | Severity | Area                                | Finding                                                                                                                                                                                                                  | Evidence                                                                                                                  | Status             | Next Step                                                                                                                                          |
| -----: | :------- | :---------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------ | :----------------- | :------------------------------------------------------------------------------------------------------------------------------------------------- |
| SR-001 | High     | Authorization (IDOR)                | Verify every controller enforces resource ownership/role checks (especially reports, sessions, excuses). Route-level `authenticate` exists, but controller-level ownership checks must be confirmed.                     | Route inventory: [ATTACK_SURFACE_MAP.csv](file:///c:/Users/shink/Pictures/absenyura/ATTACK_SURFACE_MAP.csv)               | Needs Verification | Review controllers for `req.user.id` checks + add tests for cross-user access.                                                                     |
| SR-002 | High     | CSRF                                | Confirm CSRF is enforced on all state-changing endpoints (except explicit skip list) and that clients always send `X-CSRF-Token`.                                                                                        | Middleware: [csrf.middleware.ts](file:///c:/Users/shink/Pictures/absenyura/server/middlewares/csrf.middleware.ts)         | Needs Verification | Attempt CSRF on staging using browser-based PoC; verify failures.                                                                                  |
| SR-003 | High     | Session/Cookies                     | Validate cookie flags and domain strategy on Vercel: `SameSite=None` + `Secure` + correct domain; ensure no mixed HTTP usage.                                                                                            | Cookie set: [auth.controller.ts](file:///c:/Users/shink/Pictures/absenyura/server/controllers/auth.controller.ts#L30-L55) | Needs Verification | Test on staging+prod: inspect cookie attributes + cross-origin behavior.                                                                           |
| SR-004 | High     | Secrets/Env                         | Ensure no `change_me*` values ever exist in Production/Preview env vars; ensure Preview does not have Production secrets.                                                                                                | Env template: [.env.example](file:///c:/Users/shink/Pictures/absenyura/.env.example)                                      | Needs Verification | Review Vercel env var scopes; rotate secrets; enforce via CI check.                                                                                |
| SR-005 | Medium   | Internal/Cron endpoints             | Internal endpoints use shared secrets; verify secrets are long, rotated, not logged, and protected from replay where needed.                                                                                             | Guards: [guardInternal.ts](file:///c:/Users/shink/Pictures/absenyura/server/middlewares/guardInternal.ts)                 | Needs Verification | Add monitoring for failed attempts; ensure `CRON_SECRET` required in prod.                                                                         |
| SR-006 | Medium   | Uploads                             | Public asset upload is validated as image-only (good), but also verify any locally served `/uploads/public-site` directory is not writable by untrusted users and contains no active content.                            | Static serving: [server/app.ts](file:///c:/Users/shink/Pictures/absenyura/server/app.ts#L250-L252)                        | Needs Verification | Check deployment storage + ensure only images; consider disabling local static route if unused.                                                    |
| SR-007 | Medium   | Rate limiting / DoS                 | Validate rate-limit settings against abuse patterns (refresh storms, export endpoints, cron trigger).                                                                                                                    | Rate limit: [server/app.ts](file:///c:/Users/shink/Pictures/absenyura/server/app.ts#L123-L219)                            | Needs Verification | Run bounded load tests on staging; confirm no bypass for expensive routes.                                                                         |
| SR-008 | Low      | Frontend XSS hardening              | No `dangerouslySetInnerHTML` found, but CSP and output encoding should be validated, especially for CMS-like public pages.                                                                                               | Grep: no matches for `dangerouslySetInnerHTML`                                                                            | Needs Verification | Add CSP feasibility review; check where user content is rendered.                                                                                  |
| SR-009 | High     | Security headers (static)           | Static HTML/assets responses on `hmsdp.vercel.app` miss common security headers (CSP/XFO/XCTO/Referrer-Policy/Permissions-Policy) while `/api/*` includes them. This weakens clickjacking/XSS mitigation at the edge.    | Live header probe: `/` shows `CSP=` `XFO=` `XCTO=` `Referrer-Policy=`; `/api/status` shows CSP+XFO+XCTO+no-referrer       | Needs Fix          | Add Vercel headers for HTML routes (at least X-Content-Type-Options, Referrer-Policy, X-Frame-Options or `frame-ancestors`), validate no breakage. |
| SR-010 | Medium   | Vercel config drift                 | `vercel.json` defines cache headers for `/assets/*`, but live `/assets/*.js` responses show `Cache-Control: public, max-age=0, must-revalidate` (rule not applied). This affects performance and can amplify DoS impact. | Live header probe: `/assets/index-*.js`                                                                                   | Needs Verification | Confirm deployment uses current `vercel.json`; adjust `headers`/routes or apply via Vercel project settings.                                       |
| SR-011 | Medium   | Testing constraint / device binding | API login for the provided student account is blocked by device binding (`DEVICE_BOUND`), limiting authenticated API-level testing.                                                                                      | Live API response on `/api/auth/login` for student                                                                        | Needs Decision     | Provide a fresh student test account (not device-bound) or allow a temporary device reset for the test account.                                    |
| SR-012 | Low      | Device binding UX                   | Device binding is strict: logging in with a different `device_fingerprint` returns `DEVICE_BOUND`. This is security-positive but can surprise admins/testers if they rotate devices frequently.                          | Live testing with `dwi@example.com` after reset                                                                           | Informational      | Document intended behavior and add admin SOP for reset; ensure `device_fingerprint` is stable in official clients.                                 |
